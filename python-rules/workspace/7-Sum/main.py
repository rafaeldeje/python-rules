numeros = [10, 20, 30, 40, 50]
soma = sum(numeros)
print(soma)  # Isso imprimirá a soma, que é 150.

#É usada para somar todos os elementos de uma sequência, como uma lista, tupla ou conjunto. Ela retorna a soma de todos os valores na sequência.